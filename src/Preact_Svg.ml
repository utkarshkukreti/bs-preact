let a = Preact_Core.element "a"

let animate = Preact_Core.element "animate"

let animateColor = Preact_Core.element "animateColor"

let animateMotion = Preact_Core.element "animateMotion"

let animateTransform = Preact_Core.element "animateTransform"

let circle = Preact_Core.element "circle"

let clipPath = Preact_Core.element "clipPath"

let colorProfile = Preact_Core.element "color-profile"

let cursor = Preact_Core.element "cursor"

let defs = Preact_Core.element "defs"

let desc = Preact_Core.element "desc"

let discard = Preact_Core.element "discard"

let ellipse = Preact_Core.element "ellipse"

let feBlend = Preact_Core.element "feBlend"

let feColorMatrix = Preact_Core.element "feColorMatrix"

let feComponentTransfer = Preact_Core.element "feComponentTransfer"

let feComposite = Preact_Core.element "feComposite"

let feConvolveMatrix = Preact_Core.element "feConvolveMatrix"

let feDiffuseLighting = Preact_Core.element "feDiffuseLighting"

let feDisplacementMap = Preact_Core.element "feDisplacementMap"

let feDistantLight = Preact_Core.element "feDistantLight"

let feDropShadow = Preact_Core.element "feDropShadow"

let feFlood = Preact_Core.element "feFlood"

let feFuncA = Preact_Core.element "feFuncA"

let feFuncB = Preact_Core.element "feFuncB"

let feFuncG = Preact_Core.element "feFuncG"

let feFuncR = Preact_Core.element "feFuncR"

let feGaussianBlur = Preact_Core.element "feGaussianBlur"

let feImage = Preact_Core.element "feImage"

let feMerge = Preact_Core.element "feMerge"

let feMergeNode = Preact_Core.element "feMergeNode"

let feMorphology = Preact_Core.element "feMorphology"

let feOffset = Preact_Core.element "feOffset"

let fePointLight = Preact_Core.element "fePointLight"

let feSpecularLighting = Preact_Core.element "feSpecularLighting"

let feSpotLight = Preact_Core.element "feSpotLight"

let feTile = Preact_Core.element "feTile"

let feTurbulence = Preact_Core.element "feTurbulence"

let filter = Preact_Core.element "filter"

let font = Preact_Core.element "font"

let fontFace = Preact_Core.element "font-face"

let fontFaceFormat = Preact_Core.element "font-face-format"

let fontFaceName = Preact_Core.element "font-face-name"

let fontFaceSrc = Preact_Core.element "font-face-src"

let fontFaceUri = Preact_Core.element "font-face-uri"

let foreignObject = Preact_Core.element "foreignObject"

let g = Preact_Core.element "g"

let glyph = Preact_Core.element "glyph"

let glyphRef = Preact_Core.element "glyphRef"

let hatch = Preact_Core.element "hatch"

let hatchpath = Preact_Core.element "hatchpath"

let hkern = Preact_Core.element "hkern"

let image = Preact_Core.element "image"

let line = Preact_Core.element "line"

let linearGradient = Preact_Core.element "linearGradient"

let marker = Preact_Core.element "marker"

let mask = Preact_Core.element "mask"

let mesh = Preact_Core.element "mesh"

let meshgradient = Preact_Core.element "meshgradient"

let meshpatch = Preact_Core.element "meshpatch"

let meshrow = Preact_Core.element "meshrow"

let metadata = Preact_Core.element "metadata"

let missingGlyph = Preact_Core.element "missing-glyph"

let mpath = Preact_Core.element "mpath"

let path = Preact_Core.element "path"

let pattern = Preact_Core.element "pattern"

let polygon = Preact_Core.element "polygon"

let polyline = Preact_Core.element "polyline"

let radialGradient = Preact_Core.element "radialGradient"

let rect = Preact_Core.element "rect"

let script = Preact_Core.element "script"

let set = Preact_Core.element "set"

let solidcolor = Preact_Core.element "solidcolor"

let stop = Preact_Core.element "stop"

let style' = Preact_Core.element "style"

let svg = Preact_Core.element "svg"

let switch = Preact_Core.element "switch"

let symbol = Preact_Core.element "symbol"

let text = Preact_Core.element "text"

let textPath = Preact_Core.element "textPath"

let title = Preact_Core.element "title"

let tref = Preact_Core.element "tref"

let tspan = Preact_Core.element "tspan"

let unknown = Preact_Core.element "unknown"

let use = Preact_Core.element "use"

let view = Preact_Core.element "view"

let vkern = Preact_Core.element "vkern"

let accentHeight (value : float) = Preact_Core.property "accent-height" value

let accumulate (value : string) = Preact_Core.property "accumulate" value

let additive (value : string) = Preact_Core.property "additive" value

let alignmentBaseline (value : string) = Preact_Core.property "alignment-baseline" value

let ascent (value : float) = Preact_Core.property "ascent" value

let attributeName (value : string) = Preact_Core.property "attributeName" value

let attributeType (value : string) = Preact_Core.property "attributeType" value

let azimuth (value : float) = Preact_Core.property "azimuth" value

let baseFrequency (value : string) = Preact_Core.property "baseFrequency" value

let baseProfile (value : string) = Preact_Core.property "baseProfile" value

let baselineShift (value : string) = Preact_Core.property "baseline-shift" value

let begin' (value : string) = Preact_Core.property "begin" value

let bias (value : float) = Preact_Core.property "bias" value

let calcMode (value : string) = Preact_Core.property "calcMode" value

let clipPath' (value : string) = Preact_Core.property "clip-path" value

let clipRule (value : string) = Preact_Core.property "clip-rule" value

let clipPathUnits (value : string) = Preact_Core.property "clipPathUnits" value

let color (value : string) = Preact_Core.property "color" value

let colorInterpolation (value : string) =
  Preact_Core.property "color-interpolation" value

let colorInterpolationFilters (value : string) =
  Preact_Core.property "color-interpolation-filters" value

let colorProfile' (value : string) = Preact_Core.property "color-profile" value

let colorRendering (value : string) = Preact_Core.property "color-rendering" value

let contentScriptType (value : string) = Preact_Core.property "contentScriptType" value

let contentStyleType (value : string) = Preact_Core.property "contentStyleType" value

let cursor' (value : string) = Preact_Core.property "cursor" value

let cx (value : string) = Preact_Core.property "cx" value

let cy (value : string) = Preact_Core.property "cy" value

let d (value : string) = Preact_Core.property "d" value

let diffuseConstant (value : float) = Preact_Core.property "diffuseConstant" value

let direction (value : string) = Preact_Core.property "direction" value

let display (value : string) = Preact_Core.property "display" value

let divisor (value : float) = Preact_Core.property "divisor" value

let dominantBaseline (value : string) = Preact_Core.property "dominant-baseline" value

let dur (value : string) = Preact_Core.property "dur" value

let dx (value : string) = Preact_Core.property "dx" value

let dy (value : string) = Preact_Core.property "dy" value

let edgeMode (value : string) = Preact_Core.property "edgeMode" value

let elevation (value : float) = Preact_Core.property "elevation" value

let end' (value : string) = Preact_Core.property "end" value

let externalResourcesRequired (value : bool) =
  Preact_Core.property "externalResourcesRequired" value

let fill (value : string) = Preact_Core.property "fill" value

let fillOpacity (value : string) = Preact_Core.property "fill-opacity" value

let fillRule (value : string) = Preact_Core.property "fill-rule" value

let filter' (value : string) = Preact_Core.property "filter" value

let filterUnits (value : string) = Preact_Core.property "filterUnits" value

let floodColor (value : string) = Preact_Core.property "flood-color" value

let floodOpacity (value : string) = Preact_Core.property "flood-opacity" value

let fontFamily (value : string) = Preact_Core.property "font-family" value

let fontSize (value : string) = Preact_Core.property "font-size" value

let fontSizeAdjust (value : string) = Preact_Core.property "font-size-adjust" value

let fontStretch (value : string) = Preact_Core.property "font-stretch" value

let fontStyle (value : string) = Preact_Core.property "font-style" value

let fontVariant (value : string) = Preact_Core.property "font-variant" value

let fontWeight (value : string) = Preact_Core.property "font-weight" value

let fr (value : float) = Preact_Core.property "fr" value

let from (value : string) = Preact_Core.property "from" value

let fx (value : string) = Preact_Core.property "fx" value

let fy (value : string) = Preact_Core.property "fy" value

let gradientTransform (value : string) = Preact_Core.property "gradientTransform" value

let gradientUnits (value : string) = Preact_Core.property "gradientUnits" value

let height (value : string) = Preact_Core.property "height" value

let href (value : string) = Preact_Core.property "href" value

let imageRendering (value : string) = Preact_Core.property "image-rendering" value

let in' (value : string) = Preact_Core.property "in" value

let in2 (value : string) = Preact_Core.property "in2" value

let k1 (value : float) = Preact_Core.property "k1" value

let k2 (value : float) = Preact_Core.property "k2" value

let k3 (value : float) = Preact_Core.property "k3" value

let k4 (value : float) = Preact_Core.property "k4" value

let kernelMatrix (value : string) = Preact_Core.property "kernelMatrix" value

let kernelUnitLength (value : string) = Preact_Core.property "kernelUnitLength" value

let kerning (value : string) = Preact_Core.property "kerning" value

let keySplines (value : string) = Preact_Core.property "keySplines" value

let keyTimes (value : string) = Preact_Core.property "keyTimes" value

let lengthAdjust (value : string) = Preact_Core.property "lengthAdjust" value

let letterSpacing (value : string) = Preact_Core.property "letter-spacing" value

let lightingColor (value : string) = Preact_Core.property "lighting-color" value

let limitingConeAngle (value : float) = Preact_Core.property "limitingConeAngle" value

let local (value : string) = Preact_Core.property "local" value

let markerEnd (value : string) = Preact_Core.property "marker-end" value

let markerMid (value : string) = Preact_Core.property "marker-mid" value

let markerStart (value : string) = Preact_Core.property "marker-start" value

let markerHeight (value : string) = Preact_Core.property "markerHeight" value

let markerUnits (value : string) = Preact_Core.property "markerUnits" value

let markerWidth (value : string) = Preact_Core.property "markerWidth" value

let mask' (value : string) = Preact_Core.property "mask" value

let maskContentUnits (value : string) = Preact_Core.property "maskContentUnits" value

let maskUnits (value : string) = Preact_Core.property "maskUnits" value

let max (value : string) = Preact_Core.property "max" value

let min (value : string) = Preact_Core.property "min" value

let mode (value : string) = Preact_Core.property "mode" value

let numOctaves (value : int) = Preact_Core.property "numOctaves" value

let opacity (value : string) = Preact_Core.property "opacity" value

let operator (value : string) = Preact_Core.property "operator" value

let order (value : string) = Preact_Core.property "order" value

let overflow (value : string) = Preact_Core.property "overflow" value

let overlinePosition (value : float) = Preact_Core.property "overline-position" value

let overlineThickness (value : float) = Preact_Core.property "overline-thickness" value

let paintOrder (value : string) = Preact_Core.property "paint-order" value

let pathLength (value : float) = Preact_Core.property "pathLength" value

let patternContentUnits (value : string) =
  Preact_Core.property "patternContentUnits" value

let patternTransform (value : string) = Preact_Core.property "patternTransform" value

let patternUnits (value : string) = Preact_Core.property "patternUnits" value

let pointerEvents (value : string) = Preact_Core.property "pointer-events" value

let points (value : string) = Preact_Core.property "points" value

let pointsAtX (value : float) = Preact_Core.property "pointsAtX" value

let pointsAtY (value : float) = Preact_Core.property "pointsAtY" value

let pointsAtZ (value : float) = Preact_Core.property "pointsAtZ" value

let preserveAlpha (value : bool) = Preact_Core.property "preserveAlpha" value

let preserveAspectRatio (value : string) =
  Preact_Core.property "preserveAspectRatio" value

let primitiveUnits (value : string) = Preact_Core.property "primitiveUnits" value

let r (value : string) = Preact_Core.property "r" value

let radius (value : string) = Preact_Core.property "radius" value

let refX (value : float) = Preact_Core.property "refX" value

let refY (value : float) = Preact_Core.property "refY" value

let repeatCount (value : string) = Preact_Core.property "repeatCount" value

let repeatDur (value : string) = Preact_Core.property "repeatDur" value

let requiredFeatures (value : string) = Preact_Core.property "requiredFeatures" value

let restart (value : string) = Preact_Core.property "restart" value

let result (value : string) = Preact_Core.property "result" value

let rx (value : string) = Preact_Core.property "rx" value

let ry (value : string) = Preact_Core.property "ry" value

let scale (value : float) = Preact_Core.property "scale" value

let seed (value : float) = Preact_Core.property "seed" value

let shapeRendering (value : string) = Preact_Core.property "shape-rendering" value

let specularConstant (value : float) = Preact_Core.property "specularConstant" value

let specularExponent (value : float) = Preact_Core.property "specularExponent" value

let stdDeviation (value : string) = Preact_Core.property "stdDeviation" value

let stitchTiles (value : string) = Preact_Core.property "stitchTiles" value

let stopColor (value : string) = Preact_Core.property "stop-color" value

let stopOpacity (value : string) = Preact_Core.property "stop-opacity" value

let strikethroughPosition (value : float) =
  Preact_Core.property "strikethrough-position" value

let strikethroughThickness (value : float) =
  Preact_Core.property "strikethrough-thickness" value

let stroke (value : string) = Preact_Core.property "stroke" value

let strokeDasharray (value : string) = Preact_Core.property "stroke-dasharray" value

let strokeDashoffset (value : string) = Preact_Core.property "stroke-dashoffset" value

let strokeLinecap (value : string) = Preact_Core.property "stroke-linecap" value

let strokeLinejoin (value : string) = Preact_Core.property "stroke-linejoin" value

let strokeMiterlimit (value : float) = Preact_Core.property "stroke-miterlimit" value

let strokeOpacity (value : string) = Preact_Core.property "stroke-opacity" value

let strokeWidth (value : string) = Preact_Core.property "stroke-width" value

let surfaceScale (value : float) = Preact_Core.property "surfaceScale" value

let tabindex (value : int) = Preact_Core.property "tabindex" value

let targetX (value : float) = Preact_Core.property "targetX" value

let targetY (value : float) = Preact_Core.property "targetY" value

let textAnchor (value : string) = Preact_Core.property "text-anchor" value

let textDecoration (value : string) = Preact_Core.property "text-decoration" value

let textRendering (value : string) = Preact_Core.property "text-rendering" value

let textLength (value : string) = Preact_Core.property "textLength" value

let to' (value : string) = Preact_Core.property "to" value

let transform (value : string) = Preact_Core.property "transform" value

let type' (value : string) = Preact_Core.property "type" value

let underlinePosition (value : float) = Preact_Core.property "underline-position" value

let underlineThickness (value : float) = Preact_Core.property "underline-thickness" value

let values (value : string) = Preact_Core.property "values" value

let vectorEffect (value : string) = Preact_Core.property "vector-effect" value

let version (value : float) = Preact_Core.property "version" value

let viewBox (value : string) = Preact_Core.property "viewBox" value

let visibility (value : string) = Preact_Core.property "visibility" value

let width (value : string) = Preact_Core.property "width" value

let wordSpacing (value : string) = Preact_Core.property "word-spacing" value

let writingMode (value : string) = Preact_Core.property "writing-mode" value

let x (value : string) = Preact_Core.property "x" value

let x1 (value : string) = Preact_Core.property "x1" value

let x2 (value : string) = Preact_Core.property "x2" value

let xChannelSelector (value : string) = Preact_Core.property "xChannelSelector" value

let y (value : string) = Preact_Core.property "y" value

let y1 (value : string) = Preact_Core.property "y1" value

let y2 (value : string) = Preact_Core.property "y2" value

let yChannelSelector (value : string) = Preact_Core.property "yChannelSelector" value
