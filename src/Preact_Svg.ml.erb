<% svg_elements.each do |name| %>
let <%= quote.(name) %> = Preact_Core.element "<%= unquote.(name) %>"<% end %>

<% svg_attributes.each do |(name, type)| %>
let <%= quote.(name) %> (value: <%= type %>) = Preact_Core.property "<%= unquote.(name) %>" value<% end %>
